experiment:
  name: "base64_naive_canary_language"
  description: "Base64 output attack using naive prompt injection and the canary and language defense layers."
  version: "0.1"

  input:
    dataset_path: "../../datasets/built/squad_canary_v1/dataset.json"

  target:
    name: "basic_rag"

    defense:
      type: "deconvolute"
      required_version: "0.1.0a5"
      layers:
        - type: "canary"
          enabled: true
          settings: 
            token_length: 16
        - type: "language"
          enabled: true
          settings:
            allowed_languages: ["en"]

    llm:
      provider: "openai"
      model: "gpt-4.1-mini"
      temperature: 0

    system_prompt:
      file: "system_prompts.yaml"
      key: "gakh_bahsi_baseline"

    prompt_template:
      file: "templates.yaml"
      key: "rag_standard_v1"

  evaluator:
    type: "language_mismatch"
    expected_language: "en"
    strict: true

  scenario:
    id: "base_64_naive_canary_language"
